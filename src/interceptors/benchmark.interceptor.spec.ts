import { BenchmarkInterceptor } from './benchmark.interceptor';

describe('BenchmarkInterceptor', () => {
  it('should be defined', () => {
    expect(new BenchmarkInterceptor()).toBeDefined();
  });
});
